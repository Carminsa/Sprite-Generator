My SQL..

mysql -u root -p
"enter password"

SHOW DATABASE = affiche la db

lancer la DB = mysqld_safe --skip-innodb --default-storage-engine=mysiam &

installer DB : -u root -p < base_tp1.sql



SELECT film.titre, genre.nom FROM film INNER JOIN  genre ON genre.id_genre = film.id_genre ODER BY film.id_film ASC LIMIT 15;



